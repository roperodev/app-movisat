name: app_movisat
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.3


dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^5.0.0
  latlong2: ^0.9.1
  sqflite: ^2.2.0
  path: ^1.8.3
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  sqflite_common_ffi: ^2.3.4

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true